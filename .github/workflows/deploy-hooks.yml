name: Deploy Hooks

on:
  workflow_call:
    secrets:
      BREZOR_HOOK:
        required: true

jobs:
  deploy-hooks:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Hooks
        uses: distributhor/workflow-webhook@v3
        with:
          webhook_url: $BREZOR_HOOK
